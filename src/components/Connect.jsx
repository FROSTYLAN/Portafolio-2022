import React from 'react';
import '../styles/Connect.css';

const Connect = () => {
    return (
        <section className='Connect'>
            <p>Connect</p>
        </section>
    );
};

export default Connect;