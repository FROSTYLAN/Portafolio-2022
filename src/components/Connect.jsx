import React from 'react';
import '../styles/Connect.css';

const Connect = () => {
    return (
        <div className='Connect'>
            connect
        </div>
    );
};

export default Connect;